import 'package:flutter/material.dart';

class SystemBarMetrics {
  SystemBarMetrics._();

  static double bottomInset = 0;
  static final ValueNotifier<Color?> outerBackgroundColor = ValueNotifier(null);
}
